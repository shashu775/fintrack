# Template only — replace values before applying, or generate with:
#   kubectl create secret generic fintrack-secrets -n fintrack \
#     --from-literal=POSTGRES_USER=postgres \
#     --from-literal=POSTGRES_PASSWORD=<your-password> \
#     --from-literal=JWT_SECRET=<your-jwt-secret>
apiVersion: v1
kind: Secret
metadata:
  name: fintrack-secrets
  namespace: fintrack
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: changeme
  JWT_SECRET: changeme-too
